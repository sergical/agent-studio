# Sources

The pages to open when a harness question comes up. Every URL was fetched and returned a real page on 2026-09-16 unless the row says Open. When a fact in a harness file has no row here, it came from the code or from a local machine and says so.

Rule: OpenCode facts come only from `https://opencode.ai/v2/docs` and the source repo. The v1 tree at `opencode.ai/docs` is not a source, even when v2 has no page for a topic.

## Claude Code

| Topic                                              | URL                                                | What it answers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------------------------------- | -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Settings and every key                             | https://code.claude.com/docs/en/settings-reference | `~/.claude/settings.json`, project `.claude/settings.json`, `.claude/settings.local.json`, managed settings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Config folder and env override                     | https://code.claude.com/docs/en/claude-directory   | `CLAUDE_CONFIG_DIR` overrides `~/.claude`; transcripts at `~/.claude/projects/<project>/<session>.jsonl` with `subagents/` and `tool-results/`; 30-day default retention; `.claude.json` created on first login with trust decisions; `.credentials.json`                                                                                                                                                                                                                                                                                                                                                       |
| Skills: roots, depth, links, frontmatter, switches | https://code.claude.com/docs/en/skills             | Roots: managed, `~/.claude/skills`, `.claude/skills` at the repo root, nested `<subdir>/.claude/skills` when Claude touches files there, `--add-dir`, plugin `skills/`. A symlinked skill folder loads once, deduplicated by target. `synced` is a reserved folder name. Switches: `skillOverrides` (`on`, `name-only`, `user-invocable-only`, `off`), `disable-model-invocation`, `user-invocable`, `Skill(name)` permission rules, `disableBundledSkills`                                                                                                                                                     |
| Plugins, layout and skills-dir form                | https://code.claude.com/docs/en/plugins            | Plugin root: `.claude-plugin/plugin.json` plus `skills/`, `commands/`, `agents/`, `hooks/`, `.mcp.json`, `.lsp.json`, `monitors/`, `bin/`, `settings.json`. Skills load from `skills/<name>/SKILL.md`, legacy flat `commands/*.md`, a single root `SKILL.md` when there is no `skills/`, and extra paths from the manifest `skills` field. `claude plugin init` writes `~/.claude/skills/<name>/.claude-plugin/plugin.json`, loaded as `<name>@skills-dir` with no install step. Enabled state: `enabledPlugins` in `settings.json`, keyed `plugin`, `plugin@marketplace`, `plugin@synced`, `plugin@skills-dir` |
| Plugins and marketplace                            | https://code.claude.com/docs/en/plugins-reference  | `marketplace.json`; `.claude-plugin/plugin.json`; `claude plugin install                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | uninstall | enable | disable | list | validate | init`; cache at `~~/.claude/plugins/cache/{marketplace}/{plugin}/{version}/`; synced at `~~/.claude/plugins/synced/{name}/`; data at `~/.claude/plugins/data/{plugin-id}/` |
| Install, update, version                           | https://code.claude.com/docs/en/setup              | Native installer to `~/.local/bin/claude`, Homebrew cask, npm with Node 22; `claude --version`; `claude update`; `claude doctor`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Changelog                                          | https://code.claude.com/docs/en/changelog          | Generated from https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Source                                             | https://github.com/anthropics/claude-code          | Changelog and plugin material only; the CLI is closed source, so loading behaviour comes from the docs above                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

## Codex

| Topic                                | URL                                                                                                                                                                             | What it answers                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Install, binaries, releases          | https://github.com/openai/codex                                                                                                                                                 | npm `@openai/codex`, Homebrew cask `codex`, `curl -fsSL https://chatgpt.com/codex/install.sh \| sh`, release tarballs `codex-aarch64-apple-darwin` and `codex-x86_64-apple-darwin`; `CHANGELOG.md` at the repo root; Rust source                                                                                                                                                                              |
| Config file and env override         | https://learn.chatgpt.com/docs/config-file/config-reference                                                                                                                     | `~/.codex/config.toml`, project `.codex/config.toml` only when trusted; `CODEX_HOME` overrides `~/.codex`; profiles as `$CODEX_HOME/<name>.config.toml`; `sqlite_home` for resumable state; credential store keys                                                                                                                                                                                             |
| Skills: roots, frontmatter, switches | https://learn.chatgpt.com/docs/build-skills                                                                                                                                     | Roots searched upward: `$CWD/.agents/skills`, `$REPO_ROOT/.agents/skills`, `~/.agents/skills`, `/etc/codex/skills`, plus bundled. `.codex/skills` is not listed. Frontmatter `name` and `description`; presentation and dependency metadata in `agents/openai.yaml`. Off: `[[skills.config]]` with `path` and `enabled = false`, restart required; `allow_implicit_invocation: false` in `agents/openai.yaml` |
| `--version`, `codex doctor`          | Open                                                                                                                                                                            | Not confirmed on a fetched page; pin by a local run                                                                                                                                                                                                                                                                                                                                                           |
| Sessions and rollouts                | Open                                                                                                                                                                            | Not documented on the fetched pages; the app's reader is from observed files (codex.md)                                                                                                                                                                                                                                                                                                                       |
| Plugins                              | https://github.com/openai/plugins and the `plugin-json-spec.md` sample under `codex-rs/skills/src/assets/samples/plugin-creator/references/` in https://github.com/openai/codex | `.codex-plugin/plugin.json` with optional `skills/`, `.app.json`, `.mcp.json`, `agents/`, `commands/`, `hooks.json`; default marketplace `.agents/plugins/marketplace.json`. No vendor docs page for `/plugins`, the cache layout, or enable state; third-party pages claim `~/.codex/plugins/cache/<marketplace>/<plugin>/<version>/`, unverified. See plugins.md                                            |

## OpenCode, v2 only

| Topic                                                              | URL                                 | What it answers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------ | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Plugins                                                            | https://opencode.ai/v2/docs/plugins | `plugins` array in `opencode.json(c)`: npm packages, scoped or pinned versions, local folders, or config objects; `~/.config/opencode/plugins/` and project `.opencode/plugins/` auto-discover `.ts` and `.js` files and package folders; startup loads cached package plugins, installs missing ones in the background, and checks unpinned npm and git plugins for updates. Plugins add tools, hooks, integrations, commands, agents; the page does not say skills                                                                                                                                                  |
| Config file and search order                                       | https://opencode.ai/v2/docs/config  | `~/.config/opencode/opencode.json` or `.jsonc`; project `opencode.json(c)` or `.opencode/opencode.json(c)`, searched from the working folder up to the root, direct configs merged before `.opencode` ones; a `plugins/` subfolder in the config folder                                                                                                                                                                                                                                                                                                                                                               |
| Skills: precedence, frontmatter, switches                          | https://opencode.ai/v2/docs/skills/ | Precedence: built in, then `.claude/skills` and `.agents/skills` (global, then ancestors), then `~/.config/opencode/skills`, then `.opencode/skills` from the project root toward the working folder, then the `skills` array in config (relative, `~/`, absolute, or an HTTP catalog). Frontmatter `name`, `description`, `slash`, `metadata.opencode/slash`, `metadata.opencode/autoinvoke`; `license` and `compatibility` read but unused. Off: `metadata.opencode/autoinvoke: false` hides a skill from auto-discovery; a permission rule with `"effect": "deny"` blocks a skill id. Symlink behaviour not stated |
| Env overrides                                                      | https://opencode.ai/v2/docs/config  | The fetched page states only the fixed `~/.config/opencode` path; a search snippet from the same page claims `XDG_CONFIG_HOME` and `OPENCODE_CONFIG_DIR`. Conflict, unresolved; verify in the source                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Source                                                             | https://github.com/sst/opencode     | Served as `anomalyco/opencode`, default branch `dev`; `packages/` holds the app; the skills loader file is not yet pinpointed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Install, `--version`, plugins page, sessions, first run, changelog | Open                                | No v2 page found; use the source repo and its releases, never v1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

The v2 skills page describes deny as a permission rule with an `effect` field. The app today writes `permission.skill.<name> = "deny"` (opencode_skill_permission.rs:113). Whether v2 still reads that shape must be checked in the source before the adapter lands; see opencode.md.

## pi

| Topic                                                      | URL                                 | What it answers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Skills: roots, frontmatter, switch                         | https://pi.dev/docs/latest/skills   | Roots: `~/.pi/agent/skills`, `~/.agents/skills`, project `.pi/skills` and `.agents/skills` walked up to the git root, package `skills/` folders or `pi.skills` in `package.json`, `--skill <path>`. Bare `.md` files with frontmatter count in `~/.pi/agent/skills`; elsewhere `SKILL.md` folders found recursively. Frontmatter `name` (64 chars), `description` (1024 chars), optional `license`, `compatibility`, `metadata`, `allowed-tools`, `disable-model-invocation`, modelled on agentskills.io. Off: `disable-model-invocation: true`, then `/skill:name` to invoke |
| Packages                                                   | https://pi.dev/docs/latest/packages | `pi install npm:<pkg>@<version>` and `pi install git:<host>/<path>@<ref>`; npm packages land in `~/.pi/agent/npm/` or project `.pi/npm/`, git packages in `~/.pi/agent/git/<host>/<path>` or `.pi/git/<host>/<path>`; a `pi` field in `package.json` declares `skills` as globs relative to the package root; `pi config` toggles extensions, skills, prompt templates, and themes, project settings over global                                                                                                                                                              |
| Repo, config folder, packages                              | https://github.com/badlogic/pi-mono | npm `@earendil-works/pi-coding-agent`; `~/.pi/agent/` with `settings.json` via `pi config`; packages add skills through a `"pi"` object in `package.json` and load from `node_modules`; `packages/coding-agent` owns the CLI                                                                                                                                                                                                                                                                                                                                                  |
| `--version`, env overrides, sessions, first run, changelog | Open                                | Not found on the fetched pages; sessions and `trust.json` come from observed files (pi.md, harness-detection.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## Ecosystem

| Item                        | URL                                               | What it answers                                                                                                                                                                                                                                                                                                                                                |
| --------------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| skills.sh                   | https://skills.sh                                 | Directory and `npx skills add <owner/repo>`; the homepage lists 19 agents, the CLI repo says 75, both counts recorded                                                                                                                                                                                                                                          |
| skills CLI                  | https://github.com/vercel-labs/skills             | Symlink install by default, `--copy` for independent copies; `--agent` flags with per-agent paths; `/src`, `/bin`, `/skills`, `/tests`. The lock file `~/.agents/.skill-lock.json` version 3 with `skillFolderHash` is confirmed from the code and from skills-cli-source-facts, not from a fetched page on this pass                                          |
| Agent Skills spec           | https://agentskills.io/specification              | `name` up to 64 chars, lowercase, digits, hyphens, equal to the folder name; `description` up to 1024; optional `license`, `compatibility`, `metadata`, `allowed-tools`; `scripts/`, `references/`, `assets/`; validator `skills-ref`; three stages, discovery, activation, execution                                                                          |
| Agent Skills adopters       | https://agentskills.io/home                       | The format started at Anthropic and is an open standard; the adopter list names Claude Code, Codex and ChatGPT, OpenCode, pi, Cursor, VS Code, Copilot, Gemini CLI, Goose, and more. The page says nothing about plugins                                                                                                                                       |
| Agent Plugins, announcement | https://vercel.com/blog/introducing-agent-plugins | A plugin is a folder with `plugin.json` and fixed places for its parts; only skills and MCP servers are portable, commands, hooks, and agents stay client-specific; steering committee AWS, Cursor, Microsoft, OpenAI, Vercel; launch adopters ChatGPT, Codex, Cursor, GitHub Copilot, Kiro, VS Code                                                           |
| Agent Plugins, layout       | https://agent-plugins.org                         | `plugin.json` required; `skills/` with spec-shaped skills; `mcp.json` for stdio, streamable HTTP, or legacy HTTP with SSE servers; reverse-domain extension folders such as `com.example.client/`; schemas under `/schemas` in github.com/agentplugins/agent-plugins-spec; the marketplace format and discovery procedure are on `/specification`, not fetched |
| Plugin manifest             | https://agent-plugins.org/plugin-authors/manifest | `plugin.json`: `$schema`, `name` 1 to 64 chars; optional `version`, `description`, `author`, `homepage`, `repository`, `license`, `keywords`, `extensions`; exactly one manifest per plugin root                                                                                                                                                               |
| dotagents                   | https://github.com/getsentry/dotagents            | `agents.toml` declares skills, MCP servers, hooks, subagents, plugins; `agents.lock` pins versions; `npx @sentry/dotagents init` and `install`; global `~/.agents/` or `--project`; skills in `.agents/skills` linked into each tool's folder                                                                                                                  |

## Open

- Codex: `--version` format, session storage doc, a vendor page for `/plugins` and the plugin cache, the skills loader path in `openai/codex`.
- OpenCode: install and version pages under v2, the env-override conflict, the deny rule shape, the loader path under `packages/`, whether a v2 plugin can register skills, where npm and git plugin packages are cached.
- pi: `--version`, env overrides, sessions, changelog.
- Agent Plugins: the `/specification` and `/schemas` pages for the marketplace format and discovery.
- skills CLI: a fetched page for the lock file format.
- dotagents: any native off switch.
