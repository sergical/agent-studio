# Frontend to keep

What the Claude stack #73 to #134 built in `apps/desktop/src`, `packages/ui`, and `packages/lib`, and what the new core work must not undo. Read from the PR bodies, the `docs/spec-native-feel-*.md` files, and the area files on 2026-09-17.

## The rule

The frontend has no business rule and no knowledge of harness disks. It renders the snapshot, sends one IPC call per user action, and applies events. So the rebuild changes the Rust side only. `apps/desktop/src/lib/skill-api.ts` is the one file that maps a wrapper name to a Tauri command. When a command is replaced by an `ops` function, the wrapper keeps its name and its return type, or the component that calls it changes in the same PR with a test. No component learns a new command name by hand.

## Keep as is

| Group                 | What                                                                                                                                                                                                                                                                                                                                                                                                      | PRs                    | Depends on                                                                                                                                                                                                                                         |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shell and navigation  | Command palette on Cmd+K, app shortcuts, no browser context menu or reload, compact sidebar with counts, PageShell with breadcrumb and slim header, crumb text aligned with the title                                                                                                                                                                                                                     | #95, #97               | `getSkillSnapshot` event only                                                                                                                                                                                                                      |
| Lists and rows        | 32 px rows with selection, state groups (Needs attention, Healthy, Parked) with sticky headers, row cursor with arrows and j/k, TanStack virtualization (29 rows in DOM at 378 skills), one tooltip root and one menu root per list, harness discs in `HarnessMark` and `HarnessStack`, rich tooltip with lazy location measure, Home reusing the same row, Home inbox groups, empty state, selection bar | #76, #95, #96, #97     | `getInstalledSkills`, `listSkillEvents`                                                                                                                                                                                                            |
| Keyboard and focus    | 2 px accent focus rings with 24 px hit areas, reduced-motion tokens, Escape order across palette and menus, active-list tracking, heatmap and day-details keyboard navigation                                                                                                                                                                                                                             | #95, #96, #130         | none                                                                                                                                                                                                                                               |
| Skill page            | Properties rail (Location, Harnesses, Invocation, Source, Lifecycle, Tokens), header actions in the toolbar, invocation as a segmented control, inline SKILL.md editor with compare and frontmatter repair, Escape guard on unsaved changes, location rows with repair actions                                                                                                                            | #95, #97               | `setHarnessEnabled`, `parkSkill`, `unparkSkill`, `setSkillInvocation`, `writeInstalledSkillMdIfUnchanged`, `previewSkillFrontmatterRepair`, `applySkillFrontmatterRepair`, `repairSkillLink`, `materializeHarnessRoot`, `makeSkillIndependentCopy` |
| Settings              | `SettingsCard` layout, Project folders card with patterns like `~/src/*`, Stop tracking and Remove, per-harness discovery switches, Editor card with Automatic, known apps, Choose another app, and `$EDITOR`                                                                                                                                                                                             | #110, #112, #131, #132 | `listProjectFolders`, `registerSkillProjects`, `unregisterSkillProject`, `removeSkillProject`, `getDiscoverySources`, `setDiscoverySource`, `getEditorChoices`, `setPreferredEditor`, `openSkillPath`                                              |
| Activity              | Year heatmap with Monday weeks and quantile shading, hover card, docked day-details panel, harness and trigger filters with counts, hour strip, splits, by-skill and by-project lists with paging, narrow layout with the panel between heatmap and lists, filters and open day in the store                                                                                                              | #130, #134             | `getSkillSnapshot` with `by_hour`, `listSkillEvents`, `restoreSkillEvent`                                                                                                                                                                          |
| Design tokens and CSS | Radius scale in App.css only (sm 4, md 6, lg 10, xl 14), `--color-accent-solid` for fills behind white text, `--color-bg-pressed`, motion tokens, drag region and overlay titlebar, stable scrollbar gutter with `gutter-pr-*`, select-none on chrome, arrow cursor on controls, `Kbd` component, the kit updates in `packages/ui`                                                                        | #74, #95, #96, #97     | none                                                                                                                                                                                                                                               |
| Dev harness           | Mock Tauri IPC in `dev/harness/mock-tauri.ts`, the 30-skill fixture, `?n=378` padding, `?f=1` full-year activity                                                                                                                                                                                                                                                                                          | #95, #96, #97, #130    | the mock must gain every new command in the same PR                                                                                                                                                                                                |
| Lint and tooling      | oxlint anti-slop plugin, oxfmt, react-doctor at zero findings, React Compiler on the list and row cursor with the `"use no memo"` hook                                                                                                                                                                                                                                                                    | #73, #96, #97          | plan.md section 7 adds knip, `no-restricted-imports`, Vitest                                                                                                                                                                                       |

Already dropped on the stack and staying dropped: `InstalledSkillSourceLedger` (#95) and the skills.sh access card with its status files (#97, see no-skills-sh-key-input).

## Wrappers the new stack rewires

| Today                                                                                                                                                                                              | Under the plan                                                                            | Component effect                                                 |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `getInstalledSkills`, `getSkillSnapshot`, `onSkillSnapshot`                                                                                                                                        | one snapshot DTO from `ops::scan`, plus per-operation events the store applies to one row | none; the store reducer gains an event branch (unit 0.4 test)    |
| `setDeploymentEnabled` (move to `.skill-studio-disabled/`) - **done, unit 4.4 (#175)**                                                                                                             | removed; park is the generic off switch, `setHarnessEnabled` is the native one            | the Harnesses rail offers Park instead of the hidden-folder move |
| `materializeHarnessRoot`, `materializeHarnessRootThenDisable`, `makeSkillIndependentCopy`, `repairSkillLink`, `forkSkill`, `unforkSkill`, `parkSkill`, `unparkSkill`, `removeSkill`, `updateSkill` | each one `ops` function over Stage, Swap, Link, WriteFile under the lease and the journal | same names, same return shapes; undo comes from the journal      |
| `listSkillEvents`, `restoreSkillEvent`                                                                                                                                                             | journal reads and `ops::undo`                                                             | History section unchanged                                        |
| `keepSkillTrial`, `restoreTrashedSkill`, `onTrialExpired`                                                                                                                                          | trial folds into park with a retention cap (unit 3.9)                                     | the trial banner goes                                            |
| `listSkillPacks` and the seven pack wrappers, the run wrappers                                                                                                                                     | deferred; wrappers stay, screens leave the navigation (unit 4.3)                          | none in the kept screens                                         |
| `importTrackedProjects`                                                                                                                                                                            | delete once the legacy localStorage migration has shipped in a release                    | none                                                             |

## Where each group is documented

| Group                                                               | Doc                                                                                                                                                                                         |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shell, lists, keyboard, skill page, tokens                          | `docs/spec-native-feel-frame.md`, `-lists.md`, `-keyboard.md`, `-detail.md`, `-foundations.md`, `docs/audit-motion-and-accessibility.md`, decisions in `docs/spec-native-feel-iteration.md` |
| Rows and glyphs                                                     | `docs/skill-row-design-decision.md` (#76)                                                                                                                                                   |
| Settings                                                            | `docs/spec-native-feel-iteration.md` section 6, `settings-and-projects.md` here, PR #112 and #131 bodies                                                                                    |
| Activity                                                            | PR #130 and #134 bodies only; `events-and-history.md` here predates the heatmap and must be updated                                                                                         |
| Dev harness                                                         | `docs/spec-native-feel-iteration.md` section 1                                                                                                                                              |
| Perf numbers (Home to Skills 75 to 97 ms at 378 skills, back 39 ms) | PR #96 body and the native-feel handoff notes; performance.md here carries the budgets                                                                                                      |

## Gaps

- `events-and-history.md` describes the Activity view before #130.

`apps/desktop/src/lib/skill-api.test.ts` (unit 4.4, #175) is the standing guard: it pins every skill-api.ts wrapper to its command string, cross-checks that against `src-tauri/src/lib.rs`'s `generate_handler!` list read off disk, and fails by naming the wrapper or command that drifted. Change a wrapper's name or command string only with an update to that file in the same PR.

- Bulk actions in the selection bar are a placeholder with no command behind them.
