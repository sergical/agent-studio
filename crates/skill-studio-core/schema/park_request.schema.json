{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ParkRequest",
  "description": "Request to park one universal deployment: remove its per-harness links\nand move its directory into the parked root.",
  "type": "object",
  "properties": {
    "deployment_id": {
      "description": "The universal deployment to park.",
      "$ref": "#/$defs/DeploymentId"
    }
  },
  "required": [
    "deployment_id"
  ],
  "$defs": {
    "DeploymentId": {
      "description": "Opaque deployment id.\n\nInvariant: the string starts with `dep:v1/`. Its internal layout is a core\ndetail; adapters and the UI compare it as a string and never split it.",
      "type": "string"
    }
  }
}
